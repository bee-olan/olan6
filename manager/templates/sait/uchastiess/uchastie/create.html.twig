{% extends 'sait/base.html.twig' %}

{% block title %}Создать участника{% endblock %}

{% block kroxiSait %}
    {# <a href="{{ path('sait') }}">Главная</a>
    <a href="{{ path('sait.uchastiess.uchasties') }}">Участники</a> #}

    <span class="aktiv">Создать участника</span>

{% endblock %}

{% block bodyMain %}

    <div class="card">
        <div class="card-body">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Создать участника</button>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}